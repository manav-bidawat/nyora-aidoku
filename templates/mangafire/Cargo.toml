[package]
name = "mangafire-template"
version = "0.1.0"
edition = "2021"

[dependencies]
aidoku = { git = "https://github.com/Aidoku/aidoku-rs", features = ["json"] }
nyora-common = { path = "../common" }
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }
